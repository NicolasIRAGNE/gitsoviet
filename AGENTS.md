Do not run directly. Use with docker.
.env contains OPENAI_API_KEY.
On repo, it will be in secrets.

Don't be stupid.

Use DOCKER MCP gateway to interact with GitHub. You are fully authorized to commit, push, submit PRs.
